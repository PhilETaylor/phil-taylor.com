{
  "name": "phil-taylor-com",
  "version": "1.0.0",
  "description": "Personal website for Phil E. Taylor",
  "scripts": {
    "build:css": "tailwindcss -i ./src/input.css -o ./public/styles.css --minify",
    "watch:css": "tailwindcss -i ./src/input.css -o ./public/styles.css --watch",
    "dev": "npm run build:css && npx wrangler pages dev public",
    "deploy": "npm run build:css && npx wrangler pages deploy public --project-name=phil-taylor-com",
    "start": "npm run watch:css & npx wrangler pages dev public"
  },
  "keywords": [
    "cloudflare",
    "pages",
    "personal-site"
  ],
  "author": "Phil E. Taylor",
  "license": "MIT",
  "devDependencies": {
    "tailwindcss": "^3.4.0",
    "wrangler": "^4.54.0"
  }
}
