{
  "name": "phil-taylor-com",
  "version": "1.0.0",
  "description": "Personal website for Phil E. Taylor",
  "scripts": {
    "build:css": "npx @tailwindcss/cli -i ./src/input.css -o ./public/styles.css --minify",
    "build:html": "node scripts/minify-html.js",
    "cache-bust": "node scripts/cache-bust.js",
    "watch:css": "npx @tailwindcss/cli -i ./src/input.css -o ./public/styles.css --watch",
    "build": "npm run build:css && npm run build:html",
    "dev": "npm run build:css && npx wrangler dev",
    "deploy": "npm run cache-bust && npm run build && npx wrangler deploy",
    "start": "npm run watch:css & npx wrangler dev"
  },
  "keywords": [
    "cloudflare",
    "pages",
    "personal-site"
  ],
  "author": "Phil E. Taylor",
  "license": "MIT",
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.0",
    "html-minifier-terser": "^7.2.0",
    "tailwindcss": "^4.1.0",
    "wrangler": "^4.54.0"
  }
}
